@inject NavigationManager NavigationManager

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        NavigationManager.NavigateTo("/Identity/Account/Login?ReturnUrl=/cpanel/posts", true);
    }
}
